#include	<iostream>
using namespace std;

int main() {
    int t, n, m;
    cin >> t;
    for (int i = 0; i < t; i++)
    {
        cin >> n >> m;
        for (int j = 0; j < m; j++)
        {
            int a, b;
            cin >> a >> b;
        }
        cout << n - 1 << endl;
    }
    
    return 0;
}